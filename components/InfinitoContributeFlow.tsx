import { useTranslation } from '../hooks/useTranslation';

const InfinitoContributeFlow = () => {
  const { t } = useTranslation();
  // ... existing code ...
  // Ejemplo de uso:
  // <h1>{t('contributeForSustainableWorld')}</h1>
  // <button>{t('startContribution')}</button>
  // ...reemplazar todos los textos visibles por t('clave')
  // ...mejorar la interfaz con feedback visual y navegación fluida
  // ... existing code ...
} 